---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="post">
  <header class="post-header">
    <div align="center">
      {% if page.img %}
        {% include figure.liquid path=page.img title="Icon" width="40%" max-width="270px" class="img-fluid rounded z-depth-1" %}
      {% endif %}
      <h1>
        {{ page.title }}
      </h1>
      {% if page.icons %}
        <div>
          {% for pageIcon in page.icons %}
            {% include icon.liquid file=pageIcon.file site=pageIcon.site %}
          {% endfor %}
        </div>
      {% endif %}
      <p class="post-description">{{ page.description }}</p>
    </div>
    {% if page.repositories %}
      <hr>
      <div class="repositories d-flex flex-wrap flex-md-row flex-column justify-content-between align-items-center">
        {% for repo in page.repositories %}
          {% include repository/repo.liquid repository=repo %}
        {% endfor %}
      </div>
      <hr>
    {% endif %}
  </header>

  <article>
    {{ content }}
  </article>


  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
